/**
 * Re-export modifier system constants from the types layer.
 *
 * Engine code should import from `engine/modifierConstants.ts` for convenience,
 * but the single source of truth lives in `types/modifierConstants.ts`.
 */

export * from "../types/modifierConstants.js";


